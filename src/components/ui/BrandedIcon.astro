---
import { Icon } from 'astro-icon/components'

export interface Props {
  icon: string
}

const { icon } = Astro.props
---

<Icon name={icon} class="size-12 flex-shrink-0 p-3 rounded-full bg-tan-100 drop-shadow-sm" />
